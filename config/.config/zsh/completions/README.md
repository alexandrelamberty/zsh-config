## Custom Completions

Place standalone `_command` completion files or helper scripts in this directory.
They are automatically added to `fpath` via `completion.zsh` so any `_foo`
definitions found here become available after running `compinit`.
